# This profile is activated when running in production (e.g., on Railways)
# It expects the database credentials to be provided as environment variables.

# --- Datasource ---
# The full JDBC URL is constructed from the SPRING_DATASOURCE_URL env var.
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# --- JPA / Hibernate ---
# Use "validate" in production. This checks if the schema matches the entities
# but does NOT automatically alter the database. Schema changes should be
# handled by deliberate migration scripts in a real production environment.
spring.jpa.hibernate.ddl-auto=update

# Set the PostgreSQL dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

app.jwt.secret=${APP_JWT_SECRET}